import { PendingTasksStrategy } from './pending-tasks-strategy';

describe('PendingTasksStrategy', () => {
  it('should create an instance', () => {
    expect(new PendingTasksStrategy()).toBeTruthy();
  });
});
