import { CompletedTasksStrategy } from './completed-tasks-strategy';

describe('CompletedTasksStrategy', () => {
  it('should create an instance', () => {
    expect(new CompletedTasksStrategy()).toBeTruthy();
  });
});
